A minimal flex client and minimal java server communicating via AMF.

INSTRUCTIONS:

git clone https://www.bitbucket.org/bytewood/amf-example.git
cd amf-example
mvn clean package
run.bat

Doing the above will 
- open command box with tomcat running on port 8080
- run the flex client in whatever is configured to open swf files.

